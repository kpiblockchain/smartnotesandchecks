pragma solidity ^0.4.18;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/SmartPromissoryNote.sol";

contract TestSmartPromissoryNote {

/* XXX: Not used
	function testUploadContract() public {
		SmartPromissoryNote promisoryNote = SmartPromissoryNote(DeployedAddresses.SmartPromissoryNote());
		promisoryNote.setData("test");
		// Assert.equal(promisoryNote.getData(), "test", "Checking if we get back what we put");
	}

	function testUploadContractNew() public {
		SmartPromissoryNote promisoryNote = new SmartPromissoryNote();
		promisoryNote.setData("test");
		// Assert.equal(promisoryNote.getData(), "test", "Checking");
	*/
}
